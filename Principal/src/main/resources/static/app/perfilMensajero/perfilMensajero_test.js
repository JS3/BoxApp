'use strict';

describe('myApp.perfilMensajero module', function() {

  beforeEach(module('myApp.perfilMensajero'));

  describe('perfilMensajero controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var perfilMensajeroCtrl = $controller('PerfilMensajeroCtrl');
      expect(perfilMensajeroCtrl).toBeDefined();
    }));

  });
});