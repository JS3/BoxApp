'use strict';

describe('myApp.perfilMensajero module', function() {

  beforeEach(module('myApp.viaje'));

  describe('viaje controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viajeCtrl = $controller('ViajeCtrl');
      expect(viajeCtrl).toBeDefined();
    }));

  });
});
