'use strict';

describe('myApp.productosEntregados module', function() {

  beforeEach(module('myApp.productosEntregados'));

  describe('productosEntregados controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var productosEntregadosCtrl = $controller('ProductosEntregadosCtrl');
      expect(productosEntregadosCtrl).toBeDefined();
    }));

  });
});